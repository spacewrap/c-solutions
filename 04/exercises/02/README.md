### Exercise 4.02
If `i` and `j` are positive integers, does `(-i)/j` always have the same value
as `-(i/j)`? Justify your answer.

### Answer
In C99, yes because it truncates to 0. In other versions it might differ.
